---
  - name: Playbook
    hosts: localhost
    become: yes
    become_user: root
    tasks:
      - name: ensure apache is at the latest version
        apt:
          name: httpd
          state: latest
        delegate_to: localhost
      - name: ensure apache is running
        service:
          name: httpd
          state: started
        delegate_to: localhost

